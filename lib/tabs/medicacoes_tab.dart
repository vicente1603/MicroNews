import 'package:flutter/material.dart';

class MedicacoesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Medicações",
        textAlign: TextAlign.center,
        style: TextStyle(fontSize: 34.0, fontWeight: FontWeight.bold),
      ),
    );
  }
}
